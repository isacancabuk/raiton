import { StyleSheet, View, Text } from "react-native";

export default function AdminScreen() {
  return (
    <View style={styles.container}>
      <Text>admin login successfull</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    margin: 100,
  },
});
